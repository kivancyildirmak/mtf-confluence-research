# Paribu ML sinyal araştırma hattı — bağımlılıklar
# Kurulum:  pip install -r research/requirements.txt

pandas>=2.0
numpy>=1.24
lightgbm>=4.0
shap>=0.44
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14.0

# scipy ve joblib scikit-learn ile birlikte gelir; DSR (scipy.stats.norm) ve
# model kaydı (joblib) için açıkça de belirtiliyor.
scipy>=1.10
joblib>=1.3
